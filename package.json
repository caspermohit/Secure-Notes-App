{
  "name": "secure-notes-manager",
  "version": "1.0.0",
  "scripts": {
    "build": "composer install --no-dev --optimize-autoloader && composer run build-frontend",
    "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
  },
  "engines": {
    "node": "22.16.0"
  }
}
